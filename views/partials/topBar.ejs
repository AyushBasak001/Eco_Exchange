<nav>
    <div>
        <p>Eco-Exchange</p>
    </div>
    <div>
        <% if (locals.user) { %>
            <p><strong><%= locals.user.username %></strong></p>
            <p>(<%= locals.user.role %>)</p>
        <% } else { %>
            <a href="/auth">Login</a>
        <% } %>
    </div>
</nav>